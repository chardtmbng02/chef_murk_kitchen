import "../Header/Header.css"

export const ContactHeader = () => {
 
  return (
    <section className="contactheader-banner h-96 w-full bg-yellow-50">
      <div className="flex flex-col items-center justify-center h-full"></div>
    </section>
  )
}
